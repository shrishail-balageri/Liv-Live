# Liv-Live
Building an live video streaming application1

1.Building the server
# Install NIgnix RTMP server for windows from github repo(because we require a RTMP integrated windows ready software)


#install FFmpeg
   convert rtmp module ->HLS

